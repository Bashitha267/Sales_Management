{
  "name": "admin-management-portal",
  "version": "1.0.0",
  "description": "Professional Admin Management Portal using PHP, MySQL, and Tailwind CSS",
  "scripts": {
    "dev": "php -S localhost:8000",
    "start": "php -S localhost:8000",
    "build": "echo 'No build step required for PHP'"
  }
}
